spring:
  application:
    name: cse
  servlet:
    multipart:
      enabled: true
      max-file-size: -1
      max-request-size: -1
      file-size-threshold: 256KB

logging:
  level:
    root: DEBUG
    org.mongodb: INFO
    org.springframework: INFO
    org.springframework.data.mongodb.core.convert.QueryMapper: WARN
    com.ravenherz.cse.controller: INFO
    org.thymeleaf: WARN
  file:
    name: "./logs/cse.log"

server:
  forward-headers-strategy: framework
  tomcat:
    max-http-form-post-size: -1
    max-swallow-size: -1
  error:
    path: "/error"
    whitelabel:
      enabled: false
  port: 8081