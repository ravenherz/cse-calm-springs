plugins {
    id 'java-library'
}

apply from: rootProject.file('gradle/cse-db.gradle')

description = 'CSE PostgreSQL adapter (not wired)'

dependencies {
    api project(':cse-dal-core')
    api 'org.postgresql:postgresql'
}
